# wiki-spec-page-board

Tracker: none

## The ask

Change 2 of 4 from the approved design canvas (attacca run): the capability page
gains an activity sparkline beside the title (commits that touched the spec), a
proof chip per criterion (its test, green when it resolves, amber otherwise),
Prior decisions surfaced near the top, and a Related row of capabilities the
spec's text names.

## Reading

All data either already extracted (wikiCriterion carries the proof; proofResolves
exists from wiki-home-board) or one seam away (commit dates list for the
sparkline; Prior decisions section parse; name-mention scan for Related).
