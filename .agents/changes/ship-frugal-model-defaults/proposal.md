# ship-frugal-model-defaults

Tracker: none
Queued: 2026-08-13

## The ask, verbatim

> tu ves de añadir algo mas? [...] — encola

Queued off the same 2026-08-13 token-cost brainstorm as the other two: the one lever
left living only in conversation.

## Reading

`libretto models set` and `libretto models effort` already exist, but every machine
configures them by hand and the recommendation lives nowhere. Ship the frugal
defaults in the repo — which agents run on haiku, which keep high effort (the review
and spec ends), as a documented default or a CLI preset — so the cheapest billing
lever is versioned instead of oral tradition.

Research trail: session memory `token-reduction-is-not-cost-reduction`.
