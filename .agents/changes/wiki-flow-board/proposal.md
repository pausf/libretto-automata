# wiki-flow-board

Tracker: none

## The ask

Change 4 of 4 from the approved design canvas (attacca run): the flow, measured —
where corrections surface and what waits in the queue, on its own page.

## Reading

v1 renders what the ledgers already hold: .agents/lessons.md corrections counted
by phase, and the queued proposals with their dates. The per-change cost table is
out with a named condition — libretto metrics already answers it at the CLI, and
rendering it in the wiki means reusing that walker as its own change.
