# integrate-ponytail-caveman

Tracker: none

## The ask, verbatim

> vamos a intentar incluid mas esto de https://github.com/DietrichGebert/ponytail y
> caveman, es decir mira para que se utiliza y como podriamos incluirlo mas en nuestro
> flujo para que el usuario lo tengo metido cuando de instalar sus skills, etiende? no
> mires lo que tengo yo en claude general piensa que esto es para gente que empieza de
> cero

## Reading

Investigate what ponytail (and caveman, from the same author) are for, and integrate
them more deeply into the Libretto flow so that a user who installs this repo's skills
gets them as part of the payload. The user's own `~/.claude` setup is explicitly out of
scope as a reference — the target is someone starting from zero.
