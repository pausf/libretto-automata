# wiki-command-palette

Tracker: none

## The ask

Change 3 of 4 from the approved design canvas (attacca run): a command palette —
open with the search shortcut, search across criteria, Prior decisions and
capability names, grouped results, Enter navigates to the owning page.

## Reading

Pure template work: the data (criteria texts, decisions, names) is already
extracted; it rides the page as an escaped JSON block the inline script reads.
No network, no dependency, enhancement over the existing home search.
