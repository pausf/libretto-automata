# Libretto Automata

The instructions for this repository live in `AGENTS.md`, which is the convention every
agent reads. This file exists because Claude Code reads `CLAUDE.md`, and a rule written
where nothing reads it is indistinguishable from a rule that does not exist — the
versioning section sat in `AGENTS.md` for four tags without ever reaching a session.

**Do not copy anything from `AGENTS.md` into here.** Two files kept in sync is two
sources of truth, and the one that wins is the one nobody edited.

@AGENTS.md
